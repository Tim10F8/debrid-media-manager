# Database Configuration
# WARNING: Never commit actual database credentials to version control!
DATABASE_URL="mysql://username:password@host:port/database?sslaccept=strict"

# DMM Configuration
DMM_MAX_UNCACHED_LINKS=50
DMM_ORIGIN=http://localhost:3000
REAL_DEBRID_OBSERVABILITY_SQLITE_PATH=.data/real-debrid-observability.sqlite

# TMDB API Configuration (optional)
TMDB_KEY=your_tmdb_api_key_here

# Redis Rate Limiting (optional - falls back to in-memory if not configured)
# For Docker Compose, this is set automatically via environment in docker-compose.yml
# For local dev, you can run: docker run -d -p 6379:6379 redis:7-alpine
REDIS_URL=redis://localhost:6379

# Feature Flags
NEXT_PUBLIC_ANALYTICS_ENABLED=false

# Development Only
# Uncomment to enable Eruda mobile debugger in development
# NEXT_PUBLIC_ERUDA=true

# Important Security Notes:
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env to version control
# 3. Rotate database credentials regularly
# 4. Use environment variable management services in production
# 5. Ensure .env is in your .gitignore file
